---
id: t219
name: Adversarial tests for hand-mangled task files
status: done
type: task
kind: ai
priority: 500
created: 2026-09-01T14:55:36.559Z
updated: 2026-09-01T16:27:15.465Z
created_by: session_012KfLZY3PTxW7PD51oCG2mi
history:
  - at: 2026-09-01T16:25:35.306Z
    status: in-progress
    by: session_012KfLZY3PTxW7PD51oCG2mi
  - at: 2026-09-01T16:27:15.251Z
    status: done
    by: session_012KfLZY3PTxW7PD51oCG2mi
---

Harden the store against badly formed task files, following the operator's question of 2026-09-01. What exists: frontmatter.test.ts rejects missing frontmatter, missing required fields, bad status/type enums, non-number priority, non-list id fields, and malformed history entries; doctor flags unreadable files, id-vs-filename mismatches, foreign files in tasks/, dangling references, cycles, duplicate ranks, and status inconsistencies; store.ts reads only files matching ^(t\d+)\.md$. Gaps to test and close: (1) an unclosed frontmatter block followed by a --- horizontal rule in the body — FRONTMATTER_RE (src/frontmatter.ts:9) is lazy, so body text can be absorbed as YAML and either error confusingly or silently become fields; text after a properly closed block is safe. (2) Id format: nothing validates that a hand-edited id, parent, or blocked_by entry looks like t<number>. Verify whether a mutation on a file whose frontmatter id differs from its filename (or contains path characters like ../) writes through store.path(task.id) to the wrong file — ops loads by filename but writes by task id. If it does, that is the highest-priority fix: validate id shape at parse. (3) Pin down YAML duplicate-key behavior (yaml v2 should error; assert it). (4) For each new case, assert where it surfaces: a read error in any command, or a doctor finding with the right code and fixability. TDD: red test per case in test/frontmatter.test.ts or test/doctor.test.ts, then the minimal guard.

Built: the write-through was real — store.load parsed without checking the id, and a mutation on t2.md claiming id t7 would have saved onto t7.md (or, with a path-shaped id, outside tasks/). Now parseTaskFile requires every id, parent, blocked_by and replaced_by entry to match t<number> (src/frontmatter.ts, ID_RE), and store.load refuses a file whose frontmatter id disagrees with its filename, pointing at doctor. Pinned as already safe by new tests: YAML duplicate keys throw, an unclosed frontmatter block errors instead of absorbing the body, and frontmatter-looking text after the closing delimiter stays inert body. Files: src/frontmatter.ts, src/store.ts, test/frontmatter.test.ts, test/store.test.ts. Test: npm test (454 pass).
