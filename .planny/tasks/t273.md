---
id: t273
name: Roll back the shared board
status: done
type: task
kind: ai
priority: 602
parent: t272
created: 2026-09-02T11:19:27.418Z
updated: 2026-09-02T11:23:26.389Z
created_by: fable-boards
history:
  - at: 2026-09-02T11:19:27.496Z
    status: in-progress
    by: fable-boards
  - at: 2026-09-02T11:23:26.389Z
    status: done
    by: fable-boards
---

Part of t272. Take the shared board (`planny serve --all`, t265, t267, t268)
out of the code, on the branch `wanny/boards-index`.

## What goes

Revert the code of the two feature commits, d0435be and 13590cd: the
project-scoped API routes and key scheme in src/server.ts, the `--all` and
`--root` options and the one-plan-one-board precondition in src/cli.ts, the
project picker and `apiPath` in web/, and their tests. The task files under
.planny/ that those commits touched stay as they are: the plan must not
rewind.

## What stays

src/discover.ts and test/discover.test.ts. The walk that finds every plan
is what `planny boards` (t273 and after) lists and starts. Trim it to what
that needs: the roots, not the name-and-key scheme that only the shared
board's API used.

## Done looks like

`git diff main..wanny/boards-index --stat` shows src/server.ts, src/cli.ts
and web/ back at their pre-t265 shape. `npm test` is green. This task's
record gives the numbers: lines and files removed, and where src/server.ts
ended up.

Done, 2026-09-02, on the branch wanny/boards-index (commits e7d6cf4 and 0d01b83). The revert leaves src/server.ts, src/cli.ts and web/ byte-identical to b4831e5, the commit before the shared board: 12 files, 69 lines added, 652 removed against main. src/server.ts is back from 611 lines to 502, src/cli.ts from 1141 to 1079, web/app.js from 2523 to 2423. src/discover.ts stays and shrinks from 119 lines to 86: it returns sorted store roots and no longer names or keys them. The task files under .planny/ are untouched. Tests: test/discover.test.ts (11); the shared board's tests went with it (npm test: 653 before, 631 after). How to test: in the worktree, npm test.
