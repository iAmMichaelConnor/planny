---
id: t290
name: Release 0.3.0 to npm
status: in-progress
type: task
kind: ai
priority: 563
created: 2026-09-02T18:23:25.646Z
updated: 2026-09-02T18:23:25.819Z
created_by: session_01QCxpDcnpiAWtqbBAYuD9Wp
history:
  - at: 2026-09-02T18:23:25.818Z
    status: in-progress
    by: session_01QCxpDcnpiAWtqbBAYuD9Wp
---

Ship 0.3.0 to npm. The 38 commits since 0.2.2 are a feature release, not a patch: a parked status across the CLI, the board and the doctor; board actions that act at once and offer an undo; drag a card to a new status or priority; a URL for every view and task; a keyboard pass and an accessibility pass; date-window filters; `serve --all` with a page that links every plan's board; `serve --forward` printing the ssh tunnel command; a serve port each store remembers; and a doctor that reports a rewound store.

Follow the release flow in the README: bump `package.json` (and the lock) to 0.3.0, build, test, commit, tag `v0.3.0`, push the commit and the tag, then trigger the *Publish to npm* workflow by hand (`gh workflow run publish.yml`). Publishing uses npm trusted publishing over OIDC, so no token exists locally or in CI.

Note for whoever runs this: ssh to github.com is refused from this machine (publickey). Push over HTTPS with the gh token instead: `git -c credential.helper='!gh auth git-credential' push https://github.com/iAmMichaelConnor/planny.git main --follow-tags`.

Done looks like: `npm view planny version` reports 0.3.0, the workflow run is green, and the tag is on GitHub.
