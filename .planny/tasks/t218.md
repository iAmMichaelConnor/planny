---
id: t218
name: "Act on the outcome of t216: Decide how serve logs are identified and
  cleaned up"
status: done
type: task
kind: ai
priority: 490
parent: t216
created: 2026-09-01T14:45:52.501Z
updated: 2026-09-01T14:56:10.840Z
created_by: session_012KfLZY3PTxW7PD51oCG2mi
history:
  - at: 2026-09-01T14:45:52.706Z
    status: in-progress
    by: session_012KfLZY3PTxW7PD51oCG2mi
  - at: 2026-09-01T14:56:10.439Z
    status: done
    by: session_012KfLZY3PTxW7PD51oCG2mi
---

This task records the outcome of decision t216. The operator decided: build planny serve --clean-logs, which deletes planny-serve-*.log files in the OS temp dir older than a given age; --older-than <days> sets the age, default 7 days. Operator addendum: also take proposal (b) — planny serve --stop prints where the log is kept (serve --detach already prints the log path). Keep a file whose port still answers as a live planny server. Work is TDD: failing tests first in test/detach.test.ts, then src/server.ts (cleanLogs, StopOutcome), then src/cli.ts wiring and help text, then the README. Done looks like: npm test green; the command deletes old dead logs, keeps young or live ones, and refuses --clean-logs combined with --detach or --stop. This task was created by hand because the resolve ran through the globally installed planny 0.1.9, which predates outcome-task creation.

Subsequent actions: built in this session, no further tasks spawned. planny serve --clean-logs deletes dead planny-serve-*.log files older than --older-than days (default 7) from the OS temp dir, keeps a live server's log and says so, ignores non-matching names and directories, and refuses to combine with --detach or --stop; planny serve --stop now ends 'log kept at <path>' when the log exists. Files: src/server.ts (cleanLogs, StopOutcome.log), src/cli.ts (flags, wiring, help), test/detach.test.ts (eight new cases), README.md. Test: npm test (427 pass).
