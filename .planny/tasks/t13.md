---
id: t13
name: List decisions answered since a time
status: todo
type: task
kind: ai
priority: 120
created: 2026-08-31T15:14:17.879Z
updated: 2026-08-31T15:14:17.880Z
---

An AI catching up after the operator ran `planny decide` needs the decisions answered while it was away. `planny resolve` already stamps resolved_at and appends the answer under an Outcome section, so the data exists. Add `planny decisions --resolved [--since <ISO time>] [--json]`: done decisions, newest first, filtered by resolved_at; JSON carries the full task so the AI reads the outcome without a second call. Also list what each answered decision unblocked (tasks whose last active blocker it was).
