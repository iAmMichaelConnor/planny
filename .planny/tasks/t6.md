---
id: t6
name: Add a doctor command that checks store integrity
status: done
type: task
kind: ai
priority: 50
created: 2026-08-31T14:59:26.094Z
updated: 2026-08-31T15:16:01.382Z
---

The CLI keeps the store consistent, but hand edits can break it: dangling parent or blocked_by ids, duplicate priority ranks, parent or dependency cycles, cancelled tasks with active children. `planny doctor` should list each problem with the file path and suggest the fix; `--fix` applies the safe ones (drop dangling ids, re-rank duplicates).

Built: planny doctor with 12 checks (unreadable files, id mismatches, dangling refs, duplicate ranks, parent and dependency cycles, order violations, cancelled blockers and parents, unresolved decisions) and --fix for the safe repairs. Files: src/doctor.ts, src/store.ts (scan), src/cli.ts. Tests: test/doctor.test.ts, test/store.test.ts, test/cli.test.ts — npm test. Runs at: planny doctor [--fix] [--json].
