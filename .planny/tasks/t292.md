---
id: t292
name: Release 0.3.1 to npm
status: done
type: task
kind: ai
priority: 564
created: 2026-09-02T18:42:24.199Z
updated: 2026-09-02T18:44:15.374Z
created_by: session_01QCxpDcnpiAWtqbBAYuD9Wp
history:
  - at: 2026-09-02T18:42:24.288Z
    status: in-progress
    by: session_01QCxpDcnpiAWtqbBAYuD9Wp
  - at: 2026-09-02T18:44:15.374Z
    status: done
    by: session_01QCxpDcnpiAWtqbBAYuD9Wp
---

Ship 0.3.1 to npm: a patch release carrying one fix, t291. A board rebuild no longer empties a box the reader is still typing into — the drawer's decision answer, the decisions tile's priority box, and a part-typed date window. The operator lost a decision answer to this on 0.3.0, so the fix should reach `npm update -g planny` users straight away.

Follow the release flow in the README: bump `package.json` (and the lock) to 0.3.1, build, test, commit, tag `v0.3.1`, push the commit and the tag, then trigger the *Publish to npm* workflow by hand (`gh workflow run publish.yml`).

Note: ssh to github.com is refused from this machine (publickey). Push and fetch over HTTPS with the gh token instead: `git -c credential.helper='!gh auth git-credential' push https://github.com/iAmMichaelConnor/planny.git main --follow-tags`.

Done looks like: `npm view planny version` reports 0.3.1, the workflow run is green, and the tag is on GitHub.

Shipped 2026-09-02. npm reports planny 0.3.1 as latest. The workflow run is green, CI on the release commit is green, and tag v0.3.1 is on GitHub.
