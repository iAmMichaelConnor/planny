---
id: t109
name: Adapt the publish workflow to trusted publishing
status: in-progress
type: task
kind: ai
priority: 152
created: 2026-08-31T20:16:19.020Z
updated: 2026-08-31T20:16:19.060Z
created_by: claude-fable-session-01G75wbk
history:
  - at: 2026-08-31T20:16:19.060Z
    status: in-progress
    by: claude-fable-session-01G75wbk
---

The operator configured a trusted publisher on npmjs.com (GitHub Actions, repo iAmMichaelConnor/planny, workflow publish.yml). Adapt .github/workflows/publish.yml so the OIDC path is actually used: remove the NODE_AUTH_TOKEN env from the publish step, and run on Node 24 because trusted publishing needs npm 11.5.1 or later and Node 22 bundles npm 10. Provenance is automatic under trusted publishing, so the --provenance flag can go. Requirements from https://docs.npmjs.com/trusted-publishers: id-token write permission (already present), setup-node with registry-url, package.json repository.url exactly matching the repo (already true). Done when the workflow has no reference to the secret and the next version publish succeeds via OIDC.
