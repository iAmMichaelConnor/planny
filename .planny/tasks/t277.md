---
id: t277
name: Quiet the jsdom scrollIntoView error in the UI tests
status: todo
type: task
kind: ai
priority: 642
created: 2026-09-02T11:44:08.273Z
updated: 2026-09-02T11:44:08.275Z
created_by: fable-boards
---

Every full `npm test` ends with two unhandled errors from test/ui.test.ts: `TypeError: hits[searchCursor].scrollIntoView is not a function`, thrown from moveSearchCursor in web/app.js when the search cursor moves, because jsdom has no scrollIntoView. The tests still pass, so the suite reports 'Errors 2' on every run and a real unhandled error would hide among them. Done looks like: the suite ends with no unhandled errors. Two honest fixes: stub Element.prototype.scrollIntoView in the test's boot, or guard the call in app.js (`hit.scrollIntoView?.(...)`). Seen 2026-09-02 while working t273; not caused by the shared board.
