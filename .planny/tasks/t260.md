---
id: t260
name: "Act on the outcome of t258: Decide: keep a confirm dialog on every board
  action, or switch to undo"
status: done
type: task
kind: ai
priority: 762
parent: t258
created: 2026-09-02T09:32:48.611Z
updated: 2026-09-02T09:40:08.273Z
created_by: operator
history:
  - at: 2026-09-02T09:36:42.950Z
    status: in-progress
    by: s01QCxpD
  - at: 2026-09-02T09:40:08.273Z
    status: done
    by: s01QCxpD
---

This task records the outcome of decision t258 ("Decide: keep a confirm dialog on every board action, or switch to undo"). The operator has decided; the decision and the answer follow. If the outcome calls for work, create tasks from this one, then mark this task done. The tasks that waited on the decision now wait on this task instead: t259. Update or cancel them to match the outcome. Marking this task done lifts their wait so they become workable — it does not close them.

## The decision (t258)

## Background

Every state-changing button on the board raises a browser `confirm()` dialog
first. The board calls it `guard()`, and its text always ends with the same
sentence: "Tip: an AI agent working this plan can make these changes for you
— consider asking it instead." The dialog fires on start, on done, on cancel,
on every priority move (top, bottom and a typed position), and in the
decisions tab.

This was deliberate. Task t25 ("Confirm dialogs on all status and priority
buttons") and t16 ("Confirm before drawer top/bottom bumps") both shipped, so
the pattern is the answer to an earlier question, not an oversight.

The cost shows up when the operator does several moves in a row. Reordering
five tasks means five modal dialogs and five readings of the same tip. A
browser modal also stops the page, so the operator cannot see the board while
answering the question about it.

## Why this comes to you (the operator)

You asked for these dialogs. Removing or softening them reverses a decision
you already made, and the friction is a matter of your own taste in your own
tool. It also touches the message the tool sends about who should be driving
the plan: the tip exists to push work towards the agent.

## Proposal

Keep a dialog only where the action is hard to undo, and replace the rest
with an undo. Cancel and reject keep their dialog. Start, done, todo, park
and every priority move act at once and raise a toast that says what happened
and offers "undo" for about ten seconds; undo posts the inverse through the
same API. Show the agent tip on the first action of a session only.

*Pros:* the common moves become one click; the operator sees the board change
instead of a modal; a mistake is still recoverable, and recoverable faster
than a dialog prevents it. *Cons:* it is more code than a dialog — the board
must remember the previous status or priority to post the inverse; an undo
that arrives after an agent has already acted on the change could surprise
both of them.

## Alternative options

- Keep every dialog, and drop only the repeated tip after the first one.
  Cheap, and it removes most of the irritation without new code. Weaker: the
  modal still stops the page on every move.
- Keep every dialog, and add a "don't ask again this session" checkbox, the
  way the add-task notice already works. Cheap and familiar. Weaker: an
  operator who ticks it loses the guard on cancel too.
- Change nothing.

## Needed from you

"undo", "drop the repeated tip", "add a don't-ask-again box", or "leave it".

## When

No deadline. It blocks nothing, but it touches every board action, so it is
worth answering before more buttons land (t248 adds drag-and-drop priority
moves, which would raise a dialog on every drop).

## The outcome

Cool, let's do the proposal, as long as the undo code is cleanly designed.


Subsequent actions: built the proposal, 2026-09-02. No further tasks were
needed — the decision named the work and it is all in place. t259
(cross-column drag) was waiting on this answer and is now free.

How the undo is designed, since the answer asked for that:

One function, act(label, request, inverse), owns every reversible board
action. `request` performs it; `inverse` is the request that restores what it
changed, and it is built before the change, from the task as it stands,
because afterwards the old state is gone. Both are plain {path, body} pairs
posted through the same api() every other caller uses, so an undo obeys every
rule the forward action obeys — the same lock, the same validation, the same
dependency clamp. Two builders make the inverses: inverseStatus (which
carries the wake note back only when the old status was parked, because ops
refuses the note anywhere else) and inverseBump (which asks for the position
the task holds now; bump means "end up at position N", so posting the old
number restores the old order exactly). Two callers use them: setTaskStatus
and moveTask. Every button, the drawer and the drag all go through those two.

Undo is one level deep on purpose: an undo raises no undo of its own.

Two things it does not promise, both stated in the code: a status change can
move other tasks' ranks, because the store repairs the dependency order on
every write, and undoing the status does not move those back — the invariant
put them there. And an undo can be clamped like any move, in which case ops
explains it in a warning (t246).

What kept a dialog: cancelling a task and cancelling a decision. Both rewire
or close other work, so neither has an inverse a single request can post.
Their dialog also lost the agent tip, which now appears once a visit as its
own toast on the reader's first action.

Files: web/app.js, web/style.css. Tests: test/ui.test.ts (7 new; four tests
that asserted the old confirm-first contract rewritten to the new one).
How to test: npm test (568 pass). Runs at: every board action.
